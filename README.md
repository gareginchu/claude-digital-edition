CLAUDE — Digital Edition of Babken Chookaszian

This repository contains the scaffolding for the digital scholarly edition project described in CLAUDE.md. It follows the architecture and repository layout specified in that document.

Work in progress — run `git status` and consult `CLAUDE.md` for project decisions and next steps.
