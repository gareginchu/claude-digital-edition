{
  "name": "claude-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prebuild": "node ./scripts/sync-html.mjs",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^4.0.0"
  }
}
