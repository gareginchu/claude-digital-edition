# Python
__pycache__/
*.py[cod]
*.pyo
env/
.venv/
venv/

# VS Code
.vscode/

# OS
.DS_Store
Thumbs.db

# build
dist/
build/

# data
*.sqlite
*.db

# generated enrichment output folders
tei_enriched/
tei_enriched_*/

# vendored TEI P5 RelaxNG schema (fetched in CI, cached locally)
odd/schemas/

# Claude Code local state (subagent worktrees, settings)
.claude/

# IIIF plates pipeline: rendered pages + tile hierarchy. Large binary output
# regenerated by iiif/extract_plates.py + iiif/build_tiles.py; also under
# pending-rights-clearance restrictions so must not be committed.
iiif/pages/
iiif/tiles/
# Manifest / tiles staged into the Astro public tree for local viewer review.
# Same rights constraint: do not commit.
site/public/iiif/

# Astro generated types + build artefacts + npm deps
site/.astro/
site/dist/
site/node_modules/

# ChromaDB persistent client store for dense embeddings (BGE-M3).
# Regenerated by rag/embed_dense.py; model weights cached to
# ~/.cache/huggingface/. Do not commit the vector store.
rag/chroma/

